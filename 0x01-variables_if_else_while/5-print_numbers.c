# include <stdio.h>

/**
 * main - a program that prints the alphabet in lowercase
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", 0 + i);
	}
	return (0);
}
