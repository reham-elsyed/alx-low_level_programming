# include <stdio.h>

/**
 * main - a program that prints the alphabet in lowercase
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", 0 + i);
	}
	putchar('\n');
	return (0);
}
