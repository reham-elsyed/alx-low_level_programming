#include "main.h"

/**
*_isalpha - check if character is alphabet or not
*@c: the input character
*Return:1 true else 0
*/
int _isalpha(int c)
{
if (c >= 'a' || c <= 'z' || c >= 'A' || c <= 'Z')
{
return (1);
}
else
{
return (0);
}
return ('\n');
}
